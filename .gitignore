node_modules/
dist/
.DS_Store
*.tsbuildinfo

# Base de datos local: contiene los movimientos y el token de Gmail.
server/data/
*.db
*.db-shm
*.db-wal

# Secretos
.env
.env.local

# Lo que generan las pruebas: certificados de un servidor de mentira y bases
# temporales. Los scripts sí se versionan.
server/pruebas/tls/
server/pruebas/*.db
server/pruebas/*.log
server/pruebas/galletas.txt
